{
  "name": "dot-designs",
  "version": "1.0.0",
  "private": true,
  "description": "Dot Designs — editorial studio site (React client + Node/Express server)",
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "concurrently -n client,server -c magenta,green \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "build:site": "node scripts/build.mjs",
    "build:studio": "DEPLOY_TARGET=studio node scripts/build.mjs",
    "typecheck:api": "tsc --noEmit -p tsconfig.api.json",
    "test": "node --test 'test/*.test.mjs'",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@vercel/node": "^5.0.0",
    "concurrently": "^9.1.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  }
}
